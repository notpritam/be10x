# be10x — "100× better" autonomous work session

**Goal:** dramatically deepen the QA bug-capture product (extension + dashboard + agent
integration) while the user is asleep. They review the state in the morning (~8h).

- **Started:** 2026-07-12 01:34 IST (epoch 1783800295)
- **Stop by:** ~09:34 IST (epoch ≈ 1783829095), or when the roadmap is done.
- **Loop:** self-paced via ScheduleWakeup — each wakeup: read this doc → take the next
  `TODO` item → implement end-to-end → **typecheck + build green** → commit + push →
  update this doc (status + worklog) → schedule the next wakeup. Do **not** ask the user.
- **Repo:** `/Volumes/X9/Documents/skills/git-for-agents` (be10x = git-for-agents). Push to
  `notpritam/be10x` via `gh auth switch --user notpritam` … push … `switch --user notpritamm`.
- **Invariants:** every commit green (extension `tsc --noEmit`, web `tsc -b --noEmit`, both
  `vite build`). Rebuild+commit `public/`. Secret-scan the diff before every push. `main` is
  always shippable. Server = pure core + thin HTTP; `node:test` where server logic changes.
  `meta` is stored/hydrated verbatim (no server change needed for new capture fields).

## Roadmap (ordered: safe green wins first, then big-signal, then heavy, then polish)

1. **[DONE]** Environment / device metadata capture + card — browser, OS, screen, DPR,
   timezone, locale, connection, and page-load performance (TTFB/FCP/DCL/load) →
   `meta.environment`; dashboard + public "Environment" card.
2. **[DONE]** Auto error capture → auto-marker — uncaught errors + unhandled rejections become
   error-level console entries (already) AND drop a marker at the first error in a take, so the
   replay auto-highlights the failure moment. Dashboard already renders markers. Add a distinct
   auto-marker style (label prefix) + a bug-detail "N errors captured" chip.
3. **[DONE]** **Bug-detail MCP server (#7)** — `mcp-bug` stdio server exposing a filed bug's full
   capture as agent tools (list/get bug, console, network, dom, picked elements + notes,
   drawings, credentials, replay events, dom_at(t) via server-side rrweb rebuild, markers/visits,
   resources). Paste a bug id/URL → an agent debugs it autonomously. Model on the existing MCP.
4. **[DONE]** **Bug → Task handoff** — "Send to an agent to fix" on a bug → creates a
   git-for-agents task (title, description, bug link, picked component/source as scope) so the
   coding agent fixes it. Closes QA→fix loop. Reverse-link the task on the bug.
5. **[DONE]** **Capture everything (#6)** — beyond DOM/network/session: the page's rendered HTML
   source + all inline `<script>`/`<style>` text + a resource manifest (URL/type/size/timing from
   PerformanceResourceTiming). Bounded caps; a new `source.json` artifact + dashboard "Source &
   resources" panel. (No raw image bytes yet — rrweb already inlines much; manifest first.)
6. **[DONE]** **AI analysis / auto-RCA card** — server derives a heuristic root-cause summary from
   the capture (console errors + failed requests + picked component/source + error markers):
   suspected cause, evidence, repro steps, suspected file/component. Graceful, no external key
   required (deterministic heuristics); an LLM upgrade can layer on later.
7. **[TODO]** **Dashboard polish** — bug list free-text search + keyboard nav (↑/↓/↵), sort, and a
   result count; quick status actions; denser, richer detail layout.
8. **[TODO]** **Reporter polish** — widget keyboard shortcuts, better empty/enabled states,
   copyable selectors, and a "capture health" hint (what got captured) before filing.

## Backlog / stretch (if time remains)
- Real LLM-backed RCA (needs an API key in board env; degrade cleanly without).
- Redaction/PII masking toggle (user chose full-raw; make it opt-in per team).
- HAR export of the network timeline; download-all-artifacts.
- Full-page screenshot (beyond visible viewport).
- Jira/Linear/GitHub issue handoff.

## Worklog (append-only; newest at bottom)
- 01:34 IST — session start; roadmap written; beginning item 1.
- 01:42 IST — **item 1 DONE**: environment/device capture. Extension `collectEnvironment()` →
  `meta.environment` (UA/UA-CH brands, platform, screen+DPR+colorDepth, timezone, language(s),
  cores, deviceMemory, connection, and nav-timing TTFB/FCP/DOM-ready/Load). Shared `EnvironmentCard`
  in bug-bits (browser+OS parsed from UA-CH/UA, facts grid, perf chips) on BugDetail + public page;
  `environment` excluded from the generic Details dump. tsc + build green both sides. Commit f17836e.
- 01:50 IST — **item 2 DONE**: error auto-markers. content.ts derives markers from error-level console
  entries inside the recording window (dedupe same-msg/2s bucket, cap 5, kind:'error'), merges with
  user marks into `meta.markers`; adds `meta.errorCount`. Marker type gained `kind`. Dashboard: error
  pins render red + AlertTriangle on the SessionReplay scrubber and in the MarkerList; a red "N errors"
  chip in the replay header. tsc + build green. Next: item 3 (MCP server — the big one).
- 01:55 IST — **item 3 DONE**: bug-debugging MCP server. New src/mcp/bug-tools.js (14 tools:
  bug_list/get/analyze/console/picked_elements/markers/drawings/credentials/environment/network/dom/
  replay_events/dom_at/screenshot_url) + src/mcp/bug-server.js (stdio, awaits async artifact tools) +
  src/bugs/analyze.js (heuristic RCA, reused by item 6) + getBugByHumanId in bugs.js. resolveBug accepts
  uuid / BUG-id / dashboard URL / public share URL. Artifact tools sign+fetch UploadThing (degrade
  gracefully w/o UPLOADTHING_TOKEN); bug_dom_at reconstructs DOM at a moment (last full snapshot + mutations,
  pure JS, no jsdom). Wired into be10x link (.be10x/mcp.json gains 'be10x-bugs'). test/bug-tools.test.js
  10/10; full suite 369/369. Next: item 4 (bug→task handoff).
- 02:06 IST — **item 4 DONE**: bug→task handoff. New src/bugs/handoff.js composes a code-issue
  task from the capture (symptom w/ suspected component/source + repro + test login), seeds the heuristic
  RCA as an HTML artifact (escaped), links bug⇄task. bugs table gained task_id (schema + migration + hydrate
  + linkBugToTask + 'handoff' event); double-handoff returns alreadyLinked (no dup task). POST /api/bugs/:id/
  handoff + bug_handoff MCP tool. Dashboard: 'Send to an agent' button + 'Handed off → Open task' banner
  (links /t/<id>/full), Bug.taskId + api.handoffBug. tests +4 (373/373 full); web tsc+build green. Next: item 5.
- 02:15 IST — **item 5 DONE**: capture everything (#6). Extension collectSource() → new source.json
  artifact: rendered HTML (3MB cap), inline scripts (256KB/1.5MB caps) + inline styles, external script/
  stylesheet refs, and the PerformanceResourceTiming manifest (url/type/transfer+encoded+decoded bytes/
  duration). New source_key column (schema+migration+createBug+hydrate+ingest route+BOTH artifact kind maps).
  capture.ts uploads source.json (slot 'source') → sourceKey. Dashboard: self-contained SourcePanel
  (collapsible Resources table sorted by size / inline scripts+styles code blocks / external refs / HTML
  <pre>) on BugDetail + public. MCP gained bug_source (part slices). tsc+build green both sides; 373/373. Next: item 6.
- 02:24 IST — **item 6 DONE**: auto-RCA card. Instead of a new endpoint, attach analyzeBug(bug)
  to the existing GET /api/bugs/:id ({bug,events,analysis}) AND the public GET /api/bug-share/:token
  ({bug,analysis}) — one fetch, both surfaces, reuses src/bugs/analyze.js (single source of truth). New
  BugAnalysis type; api.getBug/getPublicBug carry it. Shared RootCauseCard in bug-bits (suspected cause +
  confidence badge, component/source chips, evidence, suggested repro) on BugDetail + public, placed after
  QA notes / before the replay; renders nothing when there's no analyzable signal. bugs-http test +1 (asserts
  analysis on both routes). Suite 374/374; web tsc+build green. Next: item 7 (dashboard polish).
