node_modules/
.serve-url
.DS_Store
*.log
*.tmp

# Local deploy secrets + databases (never commit these; .env.example is the template)
.env
*.db
*.db-shm
*.db-wal
.render-check*.png
.board*.png
reference/

# TS build cache
*.tsbuildinfo
